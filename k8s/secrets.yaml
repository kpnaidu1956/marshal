# Template — replace values before applying
apiVersion: v1
kind: Secret
metadata:
  name: marshal-secrets
  namespace: marshal
type: Opaque
stringData:
  POSTGREST_JWT_SECRET: "__REPLACE_WITH_SECRET__"
  POSTGRES_PASSWORD: "__REPLACE_WITH_PASSWORD__"
  BPE_PG_PASSWORD: "__REPLACE_WITH_PASSWORD__"
  RESEND_API_KEY: ""
  RECAPTCHA_SECRET_KEY: ""
  SUPER_ADMIN_EMAILS: ""
